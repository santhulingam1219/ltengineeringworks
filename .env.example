# Database Connection (SQLite for local zero-config dev, PostgreSQL for production)
DATABASE_URL="file:./dev.db"

# Session Security (32+ character random string for iron-session / jose encryption)
SESSION_SECRET="lt_engineering_works_secret_session_key_production_grade_2026_odisha"

# Application Base URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Email Service Provider (SMTP / Resend)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="LT Engineering Works <noreply@ltengineeringworks.com>"
ADMIN_NOTIFICATION_EMAIL="ltengineeringworks7020@gmail.com"
